# ------------------------------------------------------------------------------
# Secret (Stage 4).
#
# Purpose:
# - Demonstrates how sensitive data would be injected.
# - This app does not require secrets, but DevOps pipelines often do.
#
# Notes:
# - Values are base64-encoded, not encrypted.
# - Replace with your own values and handle carefully in Git.
# ------------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: devops-learning-secret
type: Opaque
data:
  # Example placeholder (base64 of "change-me")
  EXAMPLE_TOKEN: Y2hhbmdlLW1l

